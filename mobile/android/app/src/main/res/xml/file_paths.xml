<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- FileProvider scope. Limited to the app's own external cache so the
         Share plugin can hand over generated images without granting wide
         filesystem access. -->
    <external-files-path name="share_cache" path="." />
    <cache-path name="cache" path="." />
</paths>
